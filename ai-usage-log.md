# Care Log — AI Usage Log

An open record of what Claude was used for on this project, and what it wasn't. Kept for
transparency, since this is an open-source health app — anyone should be able to see exactly
where AI was involved.

**Standing boundary:** Claude does not write final prose intended to read as the user's own voice
(marketing copy, the "why I built this" story, emails, etc.). Outlines and brainstorming are fine —
the actual writing is the user's.

---

## Log

| Date | Item | Type | Notes |
|---|---|---|---|
| 2026-08-08 | Product feature brainstorm | Brainstorm | Multi-symptom tracker concept, monetization ideas |
| 2026-08-08 | Data model design | Design | TypeScript interface sketches: Profile, Household, TrackerDefinition, IllnessTemplate, Entry, MedicationCatalogItem/Entry, Episode, StorageConfig, Entitlement, PhysicianShare, AuditEvent |
| 2026-08-08 | Security plan | Design | Local encryption approach, E2E cloud sync approach, token generation requirements, human-review cadence |
| 2026-08-08 | CI security tooling | Code (config) | `.github/dependabot.yml`, `.github/workflows/security-scan.yml` — drafted, not yet added to a real repo |
| 2026-08-08 | Data model extensions | Design | InstitutionalLicense/ProviderCode (enterprise licensing), UnitPreferences (international units) |
| 2026-08-08 | Core prototype | Code | `care-log-prototype.jsx` — passcode-derived AES-GCM local encryption, lock screen, template-driven tracker picker, medication log, on-device text report |
| 2026-08-08 | Earlier fever-tracker artifacts | Code | `fever-tracker.jsx` (with medication overlay), `storage-diagnostic.jsx` — predate the Care Log rename, same underlying build |
| 2026-08-08 | Marketing plan structure | Outline | Channel/positioning outline only — no final copy drafted, per standing boundary |
| 2026-08-08 | Demographics/units/backup decisions | Design | Profile demographic fields, unit conversion approach, backup/export design, cloud retention policy |
| 2026-08-08 | Care Log prototype build-out | Code | `care-log-prototype.jsx`: units, demographics setup step, encrypted backup/restore, fixed a silent-hang bug in profile setup, fixed sparkline y-axis (no labels → labeled) |
| 2026-08-08 | GitHub repo setup support | Support | Walked through GitHub Pages setup, `.github` folder path handling, pasted YAML content directly for phone copy-paste limitations |
| 2026-08-08 | Standalone build | Code | `care-log-standalone.html` — ported from the prototype: `window.storage` → `LocalStore` (real `localStorage`), lucide-react → inline SVG icons, no-build CDN script tags |
| 2026-08-08 | PWA scaffolding | Code | `manifest.json`, `sw.js`, three generated app icons (`icon-192.png`, `icon-512.png`, `icon-maskable-512.png`), install/offline wiring in the HTML head |
| 2026-08-08 | Sparkline y-scale fix | Code | Added visible min/max/fever-line labels to the chart; passed real `scaleMax` per tracker instead of assuming 5 |
| 2026-08-08 | Setup-profile bug fix | Code | `handleSaveProfile` was an unawaited async IIFE with no error handling — silently hung with no error message on failure; rewrote as a proper async function |
| 2026-08-08 | Medication same-time grouping | Code | Added `groupMedsByDisplayTime` — meds logged at the same minute now render as one comma-separated line instead of duplicate timestamps, applied to the report and the medication list |
| 2026-08-08 | Medication chart overlay (v1, later reworked) | Code | Ported diagonal medication labels from the earlier fever-tracker.jsx design into the new lightweight Sparkline |
| 2026-08-08 | Shared chart time domain | Code | All tracker charts + medication markers now plot on one synchronized timeline instead of each computing its own window |
| 2026-08-08 | Root cause: Babel 8 breaking change | Debug/Code | Diagnosed a blank standalone page via user-pasted raw CDN responses — unpinned `@babel/standalone` silently resolved to Babel 8, whose default JSX runtime changed to "automatic" and fails with no bundler; pinned to `@babel/standalone@7` in both the HTML and `sw.js`, bumped cache name |
| 2026-08-08 | Custom-time logging | Code | "Log for a different time" toggle added to tracker quick-log and the medication form — defaults to now, optional datetime override |
| 2026-08-08 | Editable/deletable history | Code | Full edit (value + timestamp) and delete for symptom entries via a per-tracker History list; medications got the same treatment (name, dose, timestamp editable, not just delete) |
| 2026-08-08 | Medication chart legibility iterations | Code | Several rounds: larger font, gentler angle, halo-behind-text technique, `overflow: visible` fix for edge-clipping, ultimately removed on-chart text entirely in favor of bullets + a tap-to-expand "Medications in view" list, after repeated overlap/cutoff/hard-to-read reports |
| 2026-08-08 | Pinch-zoom / pan + range presets | Code | Touch-gesture pinch/drag directly on any chart, plus 24h/3d/7d/All preset buttons — both update one shared domain so every chart stays in sync; clamped to the real data range with a minimum zoom span |
| 2026-08-08 | X-axis date/time labels | Code | Each chart now shows its visible start/end date (or date+time when the span is under 2 days) |
| 2026-08-08 | Temperature value labels | Code | Bounded label logic: label every point when there's room (≤8 points), otherwise always show just "Now" and "Peak" — never fully silent, never cluttered, at any zoom level |
| 2026-08-08 | Root cause: service worker caching strategy | Debug/Code | Diagnosed why updates weren't appearing — `sw.js` used cache-first (served stale content immediately, updated cache silently in background, needing two reloads to see a change); rewrote as network-first with cache fallback, bumped cache name again |
| 2026-08-08 | Medication marker redesign | Code | Moved medication markers out of the plot's value range into their own row below it; changed shape from filled circle to diamond for clearer visual distinction from real data points |
| 2026-08-08 | Log catch-up + process fix | Process | Caught up `ai-usage-log.md` and `decisions-log.md` after a gap; added an explicit per-turn logging rule (see below) so this doesn't recur |
| 2026-08-08 | Health concern grouping | Code | New `concerns` entity (many-to-many with trackers via `concernIds`) — trackers can belong to multiple concerns at once (e.g. a cough tagged to both "Cold/Flu" and "Allergies"). Concern selection added to the template-add flow; dashboard gets an "All / By health concern" toggle, with concern sections and an "Other" section for untagged trackers |
| 2026-08-08 | Per-dose colored medication markers | Code | Fixed: chart previously merged same-time doses into one marker/count; now each dose gets its own diamond (never merged), colored deterministically per drug name via a hash-based palette lookup, and the "Medications in view" count reflects actual doses, not time-groups. Grouped-by-time display kept only for the readable list, with colored swatches added so list entries visually match their chart diamond |
| 2026-08-08 | Medication color collision fix | Debug/Code | User reported 3 different drugs showing the same diamond color; root cause was a weak hash (`hash*31+char`) that distributes poorly when reduced mod a small power-of-two palette size (31 ≡ -1 mod 8). Replaced with FNV-1a and expanded the palette 8→10 colors; verified via an independent Python simulation that the three reported drug names now map to distinct colors before shipping |
| 2026-08-09 | Questionnaire tracker planning | Design | Verified PHQ-9/GAD-7 public domain status and scoring; flagged Goldberg Depression Scale as copyrighted (needs review); designed QuestionnaireDefinition data model with required licenseStatus and isSafetyCritical fields |
| 2026-08-09 | Licensing & clinical safety logs | Code | Created `licensing-log.md` (third-party clinical content licensing status) and `clinical-safety-log.md` (user-safety design decisions like crisis-item handling), seeded with current known items |
| 2026-08-09 | Item-9 safety policy resolved | Design | User's explicit decision: Care Log never detects or reacts to any answer, including self-harm-related ones — logged like any other data point. Updated `clinical-safety-log.md` and `decisions-log.md` to reflect this as resolved, not open |
| 2026-08-09 | Emergency resources link (deferred) | Design | User likes a bright-red, always-present, non-reactive emergency resources link (psychosis/self-harm/suicide/dehydration/poison) as a distant-future item — logged in `clinical-safety-log.md` with an accessibility note (can't rely on color alone) for whenever it's actually designed |
| 2026-08-09 | Log consistency check | Debug | Caught and fixed an editing mistake where a `clinical-safety-log.md` update had overwritten the earlier resolved item-9 row instead of adding alongside it; also caught and fixed a duplicated Notes-section line from the same edit |
| 2026-08-09 | Personal tracker limit raised | Code | User's own build: raised `MAX_TRACKERS` from 3 to 20 for personal use — no real account/entitlement system exists yet, so this is just the shared constant, not a per-user unlock. Also fixed a hardcoded "3" in the limit-reached message that wouldn't have followed the constant |
| 2026-08-09 | Custom symptom tracking implemented | Code | Built the previously-planned feature: "Add a custom symptom" entry point plus a failed-search offer (pre-filled), name+type form (scale/boolean/new Number type), reused ConcernPicker for the concern step, and a "Your trackers" reuse section in browse (searchable alongside official templates). Number type auto-scales its chart range to actual data rather than a fixed range. Deliberately did not build the rename-cascade mechanism from the planning conversation, since there's no rename UI yet to justify it — noted as a clean follow-up, not a gap |
| 2026-08-09 | Emergency Resources link entry point | Code | Added an always-present, red-styled "Emergency Resources" link to the header (visible on first-run, template picker, and dashboard), expanding to an inline placeholder panel — real resource content intentionally deferred, user will populate separately |
| 2026-08-09 | Medication color palette brightened | Code | User feedback: muted earthy palette made similar tones hard to distinguish at 9px diamond size. Swapped to 10 bright, saturated, well-spread colors, chosen to also stay distinct from the app's existing semantic colors (muted red/teal/blue). Same FNV-1a hash, same palette length — only the color values changed, so no re-verification of the collision fix needed |
| 2026-08-09 | Undo for deletion + non-destructive restore | Code | Implemented both items flagged as a standing design lens. Deletion of entries/medications changed from immediate removal to soft-delete (`deletedAt` field) — every read site (charts, history, reports, medication cap count) now routes through active-only derived views (`activeEntries`/`activeMeds`) so deleted items vanish from normal use but stay recoverable. Added a "Recently Deleted" panel (header button, only shown when non-empty) with one-tap Restore per item. Backup restore now auto-downloads an encrypted snapshot of current data (using the current key, before switching to the incoming backup's) immediately before overwriting anything — a real, durable undo path via re-import, not just a same-session convenience |
| 2026-08-09 | Flagged periods (relapse/flare marking) implemented | Code | Built the previously-planned MS relapse-marking idea, generalized beyond MS: a new `flaggedPeriods` entity (label, start/end date, tied to a concern), with a "+ Mark a period" entry point per concern in the by-health-concern view, chip display with remove, and a shaded background band on every chart for that concern's trackers (rendered behind the data line, distinct from medication markers and fever shading). Follows the same soft-delete/Recently-Deleted pattern as entries and medications, consistent with the non-destructive principle just implemented |
| 2026-08-09 | Treatments tracking implemented | Code | New "Treatments" section (ECT, ketamine/chemo infusions, physical therapy, etc.) — same shape as medications (name, detail, timestamp) but a separate list, since clinic-administered treatments are conceptually distinct from self-taken medications. Chart integration reuses the existing medication marker system rather than duplicating it: medications and treatments now share one combined "clinical events" timeline (same positioning/stagger/per-name coloring), differing only by marker shape — diamonds for medications, circles for treatments. Same soft-delete/Recently-Deleted/report-inclusion pattern as everything else |
| 2026-08-09 | Questionnaire tracker type built | Code | Generic infrastructure for score-based multi-question instruments: QUESTIONNAIRES data model, scoring/banding helpers, a real multi-question form, chart/history/report integration. License gate enforced in code (`availableQuestionnaires` filters out `needs-review`), not just written policy — PHQ-9/Goldberg still absent since neither is actually built in yet. One deliberately original, clearly-labeled placeholder instrument included so the mechanism is testable, not dormant. Repeated the exact same function-declaration-dropping mistake as last session while inserting QuestionnaireForm before ConcernPicker — caught immediately this time via the same integrity check, before regenerating or presenting anything broken |
| 2026-08-09 | Goldberg source reviewed, not reproduced | Research | Fetched the user-provided counsellingresource.com source for the Goldberg instrument. Reported back a paraphrased understanding (structure, themes, the specific self-harm item) without reproducing any of the copyrighted item text, scoring labels, or cutoff table verbatim, consistent with copyright limits. Explicitly did not add this content to QUESTIONNAIRES — reading a source doesn't change the unresolved licensing status |
| 2026-08-09 | Depression added to MS template (0-10 scale) | Code | User's decision: use a plain self-report 0-10 scale for now instead of waiting on Goldberg licensing or building real PHQ-9 content. Added directly to the MS template. Caught and fixed a real UI bug this triggered: the scale-button row had no flexWrap, so 11 buttons (0-10) would have overflowed on a phone screen — fixed in both the quick-log and history-edit rendering, not just for this one tracker |
| 2026-08-09 | Collapsible sections added | Code | Added a collapse toggle to every TrackerCard (shrinks to name + compact last-value summary, hiding chart/controls/history) and to Medications/Treatments sections (shrinks to name + count), addressing dashboard clutter now that trackers can number 15+. Caught a real JSX bug mid-build: the first TrackerCard attempt wrapped two sibling blocks in a collapse conditional without a Fragment, which Babel would reject — caught by reasoning through the actual nesting (not just the brace counter, which can't see this class of bug), fixed, and verified with a dedicated div/Fragment tag-count check across all three modified components before regenerating |
| 2026-08-09 | Treatments linked to medications | Code | Treatments can now be tagged with the medication(s) given as part of them (chip picker of known names + free-text add), shown in the treatment list line and the report. Deliberately kept as label strings, not a hard link to a specific dose entry — typing a new name doesn't create a real medication log entry, avoiding a surprising side effect. Verified div/Fragment tag balance and prop-signature match before regenerating, given the recent history of this exact class of mistake |
| 2026-08-09 | Medications and treatments linked to health concerns | Code | Closed the long-flagged gap: medications and treatments now support the same concernIds tagging trackers have had since early in the project, via the reused ConcernPicker component in both add and edit flows for each. Generalized resolveConcernIds (previously hardcoded to tracker-add state) to accept explicit args so it could be shared rather than duplicated. Dashboard payoff: the by-health-concern view now shows a compact "Medications: X" / "Treatments: Y" summary (distinct names) within each concern's section, not just grouped trackers. Verified prop signatures and div/Fragment balance on both modified section components before regenerating |
| 2026-08-09 | Passcode warning made prominent | Code | Real user confusion reported: someone set a passcode on first-run without registering it as a permanent, unrecoverable secret, then got stuck unable to unlock later. The warning text already existed but was plain gray prose easy to skim past — moved into a bold, red-bordered callout box. Kept the original explanatory sentence too rather than just swapping it out |
| 2026-08-09 | Notes feature implemented | Code | New "Notes" section for time-grounded free-text context that doesn't fit a structured tracker — same collapsible pattern as Medications/Treatments, with optional concern-tagging, backdating, edit/delete, and soft-delete recovery. Included in the report and the by-concern summary (count, not full text, to keep that view scannable). Deliberately no chart marker — a third marker shape alongside meds' diamonds and treatments' circles risked crowding the timeline rather than adding clarity |
| 2026-08-09 | Forgot-passcode / start-fresh flow | Code | Added a "Forgot your passcode?" link on the unlock screen — reveals an inline (not pop-up) warning explaining recovery genuinely isn't possible, requiring explicit confirmation before wiping the now-unreadable vault and returning to first-run setup. Caught and fixed a real gap while building this: the standalone build's storage shim only had get/set, no delete — would have crashed the very first time anyone clicked the button, since nothing had needed delete until this feature |
| 2026-08-09 | Fixed real scope ambiguity in erase warning | Code | User's friend was scared by the erase-vault warning, thinking it might wipe her whole phone. Root cause was scope ambiguity, not tone/color — "permanently erased" without saying what gets erased is a reasonable thing to misread. Pushed back on a request to make the button green (would undersell a genuinely irreversible action to real future users, not just this test). Fixed the actual problem: wording now explicitly says this only touches Care Log's own data, not the phone or any other app. Kept red/serious styling since the danger is real |
| 2026-08-09 | Fixed real data-loss race condition | Debug/Code | User reported possible data loss from logging temperature too often. Traced and confirmed: every log triggered an immediate, un-debounced save; rapid logging could start overlapping async writes, and since completion order isn't guaranteed, an older in-flight save finishing after a newer one would silently overwrite it with a stale snapshot missing the latest entry. Fixed by debouncing saves (settle 500ms, then one write of the truly latest state). Caught a new problem this introduced before shipping: a naive debounce risked losing a save entirely if the tab closed within that window, especially on mobile where cleanup isn't reliable — added a visibilitychange listener to immediately flush any pending save when the app is backgrounded or closed |
| 2026-08-09 | Bug fix: dropped ReportSection declaration | Debug | While inserting the new TreatmentSection component, an edit accidentally deleted the `function ReportSection(...)` declaration line, leaving its body orphaned. Caught via the routine brace-balance check (1465 vs 1466), traced to the exact line with a depth-tracking scan (not guessing), and confirmed fixed by verifying every function in the file starts at brace-depth zero, not just that totals matched |
| 2026-08-09 | "Track a symptom" moved to top | Code | Moved the secondary track button from after the tracker card list (where it fell off-screen with more than a couple trackers) to the top of the dashboard, before the range presets. First-run big centered button unaffected |
| 2026-08-09 | Multiple Sclerosis template + category system built | Code | Built the previously-planned MS template (14 verified trackers, core three pre-selected) and the category system it needed to be tagged correctly — added a `category` array field to every template (multi-category, not exclusive), retroactively categorized the existing 6 as "Common Illnesses," and replaced the flat browse list with category-grouped headers where a template renders under every category it belongs to. PHQ-9 not included — the questionnaire tracker type still doesn't exist as a real feature, only as a design. "Aches & Pains" (also previously planned, also not built) intentionally left out of this pass |

---

## What Claude has NOT done on this project

- Written any final marketing copy, the origin-story page, or any other user-facing prose
- Made unilateral security-sensitive decisions without flagging them (see `security-review-log.md`)
- Chosen cryptographic primitives without explaining the choice for review

Add a row here any time Claude does substantive work on this project — code, design docs, or
structural outlines — so this stays a complete record rather than a partial one.
